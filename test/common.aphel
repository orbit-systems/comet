define STACK, 100000

loc 0
IVT:
bra setup
d32 0
d64 interrupt_handler, 255

loc 2048
align 4

setup:

    li  fp, STACK
    mov sp, fp

    call ra, main

    .spin:
        bra .spin

interrupt_handler:
    .spin:
        bra .spin
;